library(vizHelp)
library(vizHelp)
library(vizHelp)
library(vizHelp)
# Add a dependency
use_package('ggplot2')
library(devtools)
# Add a dependency
use_package('ggplot2')
# (Re-)build NAMESPACE
document()
# Reload the package: CTRL-L or
load_all()
# Add a dependency
use_package('ggplot2')
# Add a dependency
use_package('ggplot2')
# Add a dependency
use_package('ggplot2')
# (Re-)build NAMESPACE
document()
# Reload the package: CTRL-L or
load_all()
library(vizHelp)
library(vizHelp)
